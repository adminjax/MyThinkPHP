请求url  http://metmastersandbox.metworld.com.cn:8093/met/informamtion  post json传输

1、资讯列表
请求参数{
	page 当前页 从1开始
	num 返回数量	
	type 1
}
返回值{
	size 资讯总数量
	informations jsonArray{
			jsonObject{
				index  
				id  资讯id
				icon 资讯icon
				title
				briefing 简介
				time 创建时间秒
				isTop 是否置顶
				type  1直播, 2 视频, 3 图文
				tagId 标签id
				
				if（type == 1）{
					roomId 房间id
				}else if（type == 2）{
					picUrl 视频封面url
					videoUrl 视频url
					videoSize 视频大小M float-----------------新
					videoTime 视频时长 秒 int-----------------新
				}else if（type == 3）{
					url 网页链接url
				}
			}
	}
}

2、删除资讯
请求参数{
	id 资讯id
	type 2
}
返回值{
	id 删除的资讯id
}
 
3、编辑资讯
请求参数{
	type 3
	id 资讯i
	icon
	title
	briefing
	time 秒int
	isTop boolean 是否置顶
	tagIds 标签id----JsonArray
	if（type == 1）{
		roomId 房间id
	}else if（type == 2）{
		picUrl 视频封面url
		videoUrl 视频url
		videoSize 视频大小M float-----------------新
		videoTime 视频时长 秒 int-----------------新
	}else if（type == 3）{
		url 网页链接url
	}
}
返回值{
	id 编辑的资讯id
}

4、新增资讯
请求参数{
	type 4
	information jsonArray数组{
		json{
			type  1直播, 2 视频, 3 图文
			if（type == 1）{
					roomId 房间id
				}else if（type == 2）{
					picUrl 视频封面url
					videoUrl 视频url
					videoSize 视频大小M float-----------------新
					videoTime 视频时长 秒 int-----------------新
				}else if（type == 3）{
					url 网页链接url
			}
			tagIds 标签id----JsonArray
			icon 
			title
			briefing
			isTop 
		}
	}
}
返回值{
	result true
	ids array新增ids
}

5、标签列表
请求参数{
	type 5
}
返回值{
	array {
		JSONObject{
			id  标签id
			desc 标签描述
		}
	}
}

6、删除标签
请求参数{
	type 6
	id 标签id
}
返回值{
	id 标签id
}

7、修改标签
请求参数{
	type 7
	id 标签id
	desc 描述
}
返回值{
	id 标签id
	desc 描述
}

8、新增标签
请求参数{
	type 8
	desc 描述
}
返回值{
	id 标签id
	desc 描述
}

